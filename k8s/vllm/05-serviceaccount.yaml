# Plain for phase 1 (weights come from huggingface.co).
# For phase 2, annotate with the IRSA role so an init container can read
# weights from S3 — no credentials anywhere:
#   eks.amazonaws.com/role-arn: <terraform output weights_reader_role_arn>
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vllm
  namespace: inference
